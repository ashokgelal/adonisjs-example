/**
 * Exposes the API to install production dependencies for a project
 * using yarn or npm
 */
export declare class Installer {
    private appRoot;
    private client;
    constructor(appRoot: string, client: 'npm' | 'yarn');
    /**
     * Install dependencies
     */
    install(): Promise<void>;
}
