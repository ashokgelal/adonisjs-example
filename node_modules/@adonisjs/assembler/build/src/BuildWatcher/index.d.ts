import { Logger } from '@poppinss/fancy-logs';
/**
 * Exposes the API to watch the output build folder and restart
 * HTTP server on changes.
 */
export declare class BuildWatcher {
    private buildRoot;
    private nodeArgs;
    private logger;
    private manifest;
    constructor(buildRoot: string, nodeArgs: string[], logger?: Logger);
    /**
     * Returns true when filePath is .js or .json. We need this to
     * restart the HTTP server
     */
    private isScriptFile;
    /**
     * Watch for compiled output changes
     */
    watch(buildDir: string, poll?: boolean): Promise<void>;
}
