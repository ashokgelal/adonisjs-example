"use strict";
/*
* @adonisjs/assembler
*
* (c) Harminder Virk <virk@adonisjs.com>
*
* For the full copyright and license information, please view the LICENSE
* file that was distributed with this source code.
*/
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const has_yarn_1 = __importDefault(require("has-yarn"));
const ace_1 = require("@adonisjs/ace");
/**
 * Compile typescript project Javascript
 */
class Build extends ace_1.BaseCommand {
    /**
     * Invoked automatically by ace
     */
    async handle() {
        const { Watcher } = await Promise.resolve().then(() => __importStar(require('../src/Watcher')));
        const { Compiler } = await Promise.resolve().then(() => __importStar(require('../src/Compiler')));
        const { ADONIS_ACE_CWD, ADONIS_IS_TYPESCRIPT } = await Promise.resolve().then(() => __importStar(require('../config/env')));
        const cwd = ADONIS_ACE_CWD();
        /**
         * Dis-allow when CWD is missing. It will always be set by `node ace`
         * commands and also when project is not a typescript project.
         */
        if (!cwd || !ADONIS_IS_TYPESCRIPT()) {
            this.logger.error('Cannot build a non-typescript project. Make sure to run "node ace build" from the project root');
            return;
        }
        /**
         * --watch and --production flags aren't allowed together
         */
        if (this.watch && this.production) {
            this.logger.info('--watch and --production flags cannot be used together. Skipping --watch');
        }
        /**
         * Deciding the client to use for installing dependencies
         */
        this.client = this.client || has_yarn_1.default(cwd) ? 'yarn' : 'npm';
        if (this.client !== 'npm' && this.client !== 'yarn') {
            this.logger.warn('--client must be set to "npm" or "yarn"');
            return;
        }
        try {
            if (this.production) {
                await new Compiler(cwd, false, [], this.logger).compileForProduction(this.client);
            }
            else if (this.watch) {
                await new Watcher(cwd, false, [], this.logger).watch(this.poll);
            }
            else {
                await new Compiler(cwd, false, [], this.logger).compile();
            }
        }
        catch (error) {
            this.logger.fatal(error);
        }
    }
}
Build.commandName = 'build';
Build.description = 'Compile typescript code to Javascript. Optionally watch for file changes';
__decorate([
    ace_1.flags.boolean({ description: 'Watch filesystem and re-compile changes', alias: 'w' }),
    __metadata("design:type", Boolean)
], Build.prototype, "watch", void 0);
__decorate([
    ace_1.flags.boolean({ description: 'Build for production', alias: 'prod' }),
    __metadata("design:type", Boolean)
], Build.prototype, "production", void 0);
__decorate([
    ace_1.flags.string({ description: 'Select the package manager to decide which lock file to copy to the build folder' }),
    __metadata("design:type", String)
], Build.prototype, "client", void 0);
__decorate([
    ace_1.flags.boolean({
        description: 'Detect file changes by polling files instead of listening to filesystem events',
        alias: 'p',
    }),
    __metadata("design:type", Boolean)
], Build.prototype, "poll", void 0);
exports.default = Build;
